app-id: org.ssoc.ugit
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk: org.freedesktop.Sdk
command: ugit
finish-args:
  - --filesystem=home:rw
modules:
  - name: fzf
    buildsystem: simple
    build-options:
      build-args:
        - --share=network
    build-commands: 
      - wget https://github.com/junegunn/fzf/releases/download/0.33.0/fzf-0.33.0-linux_amd64.tar.gz
      - tar -xvf fzf-0.33.0-linux_amd64.tar.gz
      - install -Dm755 fzf ${FLATPAK_DEST}/bin/fzf
    # sources:
    #   - type: archive
    #     url: https://github.com/junegunn/fzf/releases/download/0.33.0/fzf-0.33.0-linux_amd64.tar.gz
    #     sha256: bcf2f4ba957cce15cea1546f48cdd3bfe9b5dc9a6e80ccc400314bda171466f3

  - name: git
    buildsystem: simple
    build-commands:
      - echo ${FLATPAK_DEST}
      - install -Dm755 $(which git) ${FLATPAK_DEST}/bin/
      - install -Dm755 $(which git-cvsserver) ${FLATPAK_DEST}/bin/
      - install -Dm755 $(which git-receive-pack) ${FLATPAK_DEST}/bin/
      - install -Dm755 $(which git-shell) ${FLATPAK_DEST}/bin/
      - install -Dm755 $(which git-upload-archive) ${FLATPAK_DEST}/bin/
      - install -Dm755 $(which git-upload-pack) ${FLATPAK_DEST}/bin/         

 
  - name: ugit
    buildsystem: simple
    build-options:
      build-args:
        - --share=network
    build-commands:
      - curl -fsSL https://raw.githubusercontent.com/Bhupesh-V/ugit/master/ugit -o ugit
      - chmod +x ugit
      - install -D ugit /app/bin/ugit
    # sources:
    #   - type: file
    #     url: https://github.com/Bhupesh-V/ugit/releases/download/v5.3/ugit
